// string type - primitive
let greetings: string = "Hello ! How are you ?";
console.log(`My greeting to you : ${greetings}`);

// number type - primitive
let userId: number = 67893298143;

// boolean type - primitive
let isPaidUser: boolean = true;

// but, do not declare type, for to obvious types like these, especially if initiatized also
// do not use 'any' in the type declaration